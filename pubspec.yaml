name: kv_design_token
description: "A new Flutter project."
version: 1.0.0
homepage:

environment:
  sdk: ^3.5.3
  flutter: ">=3.24.3"

dependencies:
  flutter:
    sdk: flutter
  diacritic: any

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: any